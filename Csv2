const crearArray = (numero) => {
  // Usando el método Array.from para crear un array con 5 elementos
  // y map para llenar cada elemento con el valor correspondiente
  const nuevoArray = Array.from({ length: 5 }, (_, index) => numero + index + 1);
  return nuevoArray;
};

// Ejemplo de uso
const numeroInicial = 10;
const resultado = crearArray(numeroInicial);

console.log(resultado); // Output: [11, 12, 13, 14, 15]


// ...

const renderizarBotones = () => {
  const contenedorBotones = document.getElementById('paginacion');

  // Limpiar los botones existentes
  contenedorBotones.innerHTML = '';

  const paginasMostradas = 3;
  let inicio = Math.max(1, paginaActual - 1);
  const fin = Math.min(totalPaginas, inicio + paginasMostradas - 1);

  if (paginaActual > totalPaginas - paginasMostradas + 1) {
    inicio = Math.max(1, totalPaginas - paginasMostradas + 1);
  }

  for (let i = inicio; i <= fin; i++) {
    const boton = document.createElement('button');
    boton.innerText = i;
    boton.addEventListener('click', () => {
      paginaActual = i;
      mostrarRegistros();
      renderizarBotones();
    });
    contenedorBotones.appendChild(boton);
  }
};

// ...
