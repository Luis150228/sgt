tengo la siguiente funcion y requiero que cuando sean las 18 hrs le reste un dia a la fecha

export const mostrarTiempo = ()=>{
    const ahora = new Date()
    // const fecha = ahora.toLocaleDateString();
    const tiempo = ahora.toLocaleTimeString()
    const fecha = ahora.toISOString().split('T')[0]
    // const tiempo = ahora.toISOString().split('T')[1]
    const printDate = `${fecha} ${tiempo}`

    return printDate
}
export const mostrarTiempo = () => {
  const ahora = new Date();

  if (ahora.getHours() >= 18) {
    // Restar un día a la fecha
    ahora.setDate(ahora.getDate() - 1);
  }

  const fecha = ahora.toISOString().split("T")[0];
  const tiempo = ahora.toLocaleTimeString();
  const printDate = `${fecha} ${tiempo}`;

  return printDate;
};
