const data = [
    // Tus datos aquí...
];

const groupedByAdf = {};

// Recorremos cada objeto del arreglo
for (const item of data) {
    const adf = item.adf;

    // Si no existe un objeto asociado al ADF, lo inicializamos con el item actual
    if (!groupedByAdf[adf]) {
        groupedByAdf[adf] = item;
    }
}

console.log(groupedByAdf);
