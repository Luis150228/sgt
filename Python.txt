const data = [
    // El array proporcionado va aquí
];

const uniqueBrands = [];
const brandsSet = new Set();

for (const item of data) {
    if (!brandsSet.has(item.marca)) {
        brandsSet.add(item.marca);
        uniqueBrands.push(item.marca);
    }
}

console.log(uniqueBrands);
