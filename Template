btnModal.addEventListener('click', (e) => {
    const frm = document.getElementById('frm-adf-trancit');
    const data = Object.fromEntries(new FormData(frm));
    const orden = e.target.dataset.orden;
    console.log(data);
    console.log(orden);
});

en este codigo estoy recolectando los datos de un formulario y quiero agregar a esos datos el valor de orden 

la variable data trae los siguientes datos 

{
    "ide": "1120010447",
    "num-adf": "12345678900000003",
    "usr-envia": "CAUSUR",
    "almacen-origen": "CAU SUR"
}

de que forma le agrego un elemento mas en este caso orden