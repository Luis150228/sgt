TENGO LA SIGUIENTE TABLA CON VALORES EN TODOS LOS CAMPOS MENOS EN KS Y QUIERO ACTUALIZARLA CON UNA CONSULTA, LOS CAMPOS reporte, semana, resuelto FORMAN EL INDEX UNICO, POR SE DEBE ACTUALIZAR SOLO DONDE ESTOS CAMPOS SEAN IGUALES, MARIA DB COMO LO DEBO HACER

SELECT reporte, semana, resuelto, atendidos, enTiempo, excepciones, ks, tiempo, limiteKs FROM eut_reportesbk.eut_weeklyreport;

SELECT reporte, semana, resuelto, SUM(K) as 'k' FROM (SELECT `Ticket` as 'folios', date(`Fecha resolucion`) AS 'resuelto',  `Zona` as 'zona', `Tiempo` as 'tiempo', 1 as 'atendidos', IF(reporte like '%K', 1, 0) as 'K', `Inhibidor` as 'inhibidor', IF(`CCG`= 'En Metrica', 1, 0) as 'ccg', `SEMANAL` as 'semana', `REPORTE` as 'reporte' FROM eut_weeklyreportsinc)cauInck WHERE semana = '03 NOV - 09 NOV' AND reporte like 'CAU_INC%' group by resuelto;