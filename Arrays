const graphModelo = {
  "UC300": 50,
  "LATITUDE 5400": 24,
  "LATITUDE 5410": 189,
  "LATITUDE 7400": 55,
  "LATITUDE 7480": 16,
  "B-57001": 60,
  "E5450": 3
};

const transformedArray = [];

for (const [modelo, cantidad] of Object.entries(graphModelo)) {
  transformedArray.push([modelo, cantidad]);
}

console.log(transformedArray);
