export const buscarElementos = (criterio, valor, data) => {
    console.log(criterio);
    console.log(valor);
    console.log(data);

    const resultados = [];

    for (const elemento of data) {
        // Verifica si el criterio existe en el elemento y si es una cadena
        if (elemento[criterio] !== undefined && typeof elemento[criterio] === 'string' &&
            elemento[criterio].toLowerCase().indexOf(valor.toLowerCase()) !== -1) {
            resultados.push(elemento);
        }
    }

    return resultados;
};
