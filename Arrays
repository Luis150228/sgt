puedes ajustar eso a esta consulta este es el campo de tipo de equipo 'tipo_equipo', 

SELECT * FROM (SELECT i.noe, oe.num_pedido, oe.stock_adquirido, i.entrada_Type 'grupo_equipo', e.marca, e.tipo_equipo, e.modelo, i.almacen, i.stock, i.usr_resguardo, em.empresa, i.num_etiqueta, i.serie, i.adf, i.observacion, i.f_modifica, i.f_registro FROM almacen_inventario i 
		LEFT JOIN almacen_catalogoequipos e on i.idequipo = e.idequipo
		LEFT JOIN almacen_empresas em on i.empresa = em.id
		LEFT JOIN almacen_almacenes a on i.almacen = a.nombre
		LEFT JOIN `almacen_ordenentrada` oe on i.noe = oe.noe)almacenes GROUP BY almacen;
